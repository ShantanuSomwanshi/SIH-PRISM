# ---------------------------------------------------------------
# PRISM backend dependencies
# Install:  pip install -r requirements.txt
# ---------------------------------------------------------------

# --- Web API ---
fastapi[standard]>=0.115.0        # includes uvicorn
pydantic>=2.9.0
python-dotenv>=1.0.1              # reads backend/.env

# --- LangChain ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0        # PDFPlumberLoader lives here
langchain-text-splitters>=0.3.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0      # the embedding wrapper we standardise on
langchain-groq>=0.2.0

# --- Vector store ---
chromadb>=0.5.0

# --- Embeddings and reranking ---
sentence-transformers>=3.0.0      # BAAI/bge-m3 + cross-encoder

# --- Keyword search ---
rank-bm25>=0.2.2

# --- GeM category page scraping ---
requests>=2.32.0
beautifulsoup4>=4.12.3

# --- PDF text extraction ---
pdfplumber>=0.11.0

# --- OCR for scanned pages (Phase 1) ---
pytesseract>=0.3.13               # needs the Tesseract program installed separately
PyMuPDF>=1.24.0                   # renders PDF pages to images for OCR
Pillow>=10.4.0

# --- LLM client ---
groq>=0.11.0
